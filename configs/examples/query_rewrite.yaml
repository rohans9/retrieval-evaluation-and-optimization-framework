device: auto
logging:
  level: INFO
retrieval:
  retriever: hybrid
  top_k: 8
query_enhancement:
  enabled: true
  methods:
    - rewrite
  rewrite:
    backend: template
    max_new_tokens: 32
reranking:
  enabled: false
